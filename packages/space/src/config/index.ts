/**
 * ==========================================
 * 这里也是常量的导出，但是和 constants 有所不同
 * 1. constants 是全局都可以引入的, config 只能在主进程中使用
 * 2. config 常量更多的是根据不同的状态创建导出的常量, 是动态的, 并且是不希望被编辑修改的
 * ==========================================
 */

export * from './pages';

